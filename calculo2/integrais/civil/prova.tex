\documentclass{prova}

\renewcommand{\sin}{\mbox{sen}}

\professor{Prof. Adriano Barbosa}
\disciplina{C\'alculo Diferencial e Integral II}
\avaliacao{P1}
\curso{Eng. Civil}
\data{09/11/2017}

\begin{document}
	\cabecalho{5}  % o numero 5 indica a qnt de quadros na tabela de nota

	\textbf{Todas as respostas devem ser justificadas.}
	\begin{questionario}
		\q{Resolva a integral definida $\displaystyle\int_0^{\pi/2} \sin(\theta)\, e^{\cos(\theta)}\ d\theta$.}
		\q{Calcule a integral $\displaystyle\int_0^a e^y \cos(a-y)\ dy$, onde $a$ \'e uma constante.}
		\q{Dados $p(x) = x^2+8x-3$ e $q(x) = x^3+3x^2$:}
			\begin{questionario}
				\qq{Fatore $q(x)$.}
				\qq{Escreva $\displaystyle\frac{p(x)}{q(x)}$ como soma de fra\c{c}\~oes parciais.}
				\qq{Calcule a integral $\displaystyle\int \frac{p(x)}{q(x)}\ dx$.}
			\end{questionario}
		\q{Determine se a integral impr\'opria $\displaystyle\int_2^\infty
			\frac{dx}{x\ln{x}}$ \'e convergente ou divergente e calcule seu
			valor, se poss\'{\i}vel.}
		\q{Resolva a integral indefinida $\displaystyle\int e^{\sqrt{x}}\ dx$.}
	\end{questionario}
\end{document}
