\documentclass{prova}

\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{gensymb}

\setlength{\textheight}{25cm}

\renewcommand{\sin}{\,\mbox{sen}\,}
\newcommand{\ds}{\displaystyle}

\professor{Prof.\@ Adriano Barbosa}
\disciplina{C\'alculo Diferencial e Integral III}
\avaliacao{PS}
\curso{Engenharia Mecânica}
\data{12/06/2021}

\begin{document}
	\cabecalho{5}  % o numero 5 indica a qnt de quadros na tabela de nota

    \textbf{Todas as respostas devem ser justificadas.}

    \textbf{Resolva apenas a avalia\c{c}\~ao referente a sua menor nota.}
    \vspace{0.5cm}

    {\bf Avalia\c{c}\~ao P1:}
    \begin{questionario}
        \q{(1,5 pt) Determine se a função $f(x,y)=\left\{\begin{array}{lr}
            \ds\frac{y^4-x^8}{x^4+y^2}, & (x,y)\neq (0,0) \\
            0, & (x,y) = (0,0)
            \end{array}\right.$
            é contínua em $(0,0)$.}
        \q{(2,5 pt) Seja $f$ tal que $f(zx, zy) = z^2 f(x,y),\forall z>0$. Mostre que}
            \[x\frac{\partial f}{\partial x}(zx,zy) + y\frac{\partial
                f}{\partial y}(zx, zy) = 2f(zx,zy)\]
        \q{Dados os pontos $(1,1)$, $(3,5)$, $(5,4)$ e $f(x)=ax+b$. Seja }
            \begin{questionario}
                \qq{(1,5 pt) Encontre os valores de $a$ e $b$ tais que
                    \[(f(1)-1)^2 + (f(3)-5)^2 + (f(5)-4)^2\]
                    seja mínimo.}
                \qq{(2,0 pt) Interprete o que significa minimizar o valor de
                    \[F(a,b) = (f(1)-1)^2 + (f(3)-5)^2 + (f(5)-4)^2.\]}
            \end{questionario}
        \q{(2,5 pt) Use o método dos multiplicadores de Lagrange para encontrar os
            valores máximo e mínimo de $f(x,y)=(xy)^2-1$ restrito a curva
            $x^2+6y^2=12$.}
        %\q{(2 pt) Classifique os pontos críticos de $F(x,t)=9x^2-12xy+4t^2$.}
    \end{questionario}

\end{document}
